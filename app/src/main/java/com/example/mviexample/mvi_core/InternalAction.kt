package com.example.mviexample.mvi_core

/**
 * `Internal Action` is an internal "`Intent`" that is more detailed than the [Action]. It is
 * created by [Store] and consumed by [Store] too (in the "reduce" function).
 */
abstract class InternalAction {
}